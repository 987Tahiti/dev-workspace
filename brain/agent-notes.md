# Brain / Notes IA

## Objectifs

- Utiliser `c:\Dev` comme dossier principal.
- Gérer les projets Android, Web et Windows.
- Installer et configurer Git, GitHub CLI, Visual Studio Code, Android Studio, Node.js et .NET.
- Mettre en place des agents IA et un système de rotation en cas de limite de session ou de token.

## Actions réalisées

- Création des dossiers `projects\Android`, `projects\Web`, `projects\Windows`, `agents`, `brain`.
- Ajout de `setup.ps1` et `README.md`.
- Ajout d’un script de suivi IA : `agents\ai-monitor.ps1`.

## Prochaines étapes

1. Installer `GitHub CLI` et `VS Code` si non présents.
2. Initialiser un dépôt Git local dans `c:\Dev`.
3. Configurer `gh auth login` pour GitHub.
4. Créer un script pour basculer automatiquement entre IA.
5. Vérifier les limites réelles de session/token des API utilisées.
